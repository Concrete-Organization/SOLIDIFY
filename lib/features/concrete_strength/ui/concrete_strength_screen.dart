import 'package:flutter/material.dart';

class ConcreteStrengthScreen extends StatelessWidget {
  const ConcreteStrengthScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('chatbot'),
      ),
    );
  }
}
